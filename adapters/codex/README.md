# Codex CLI Adapter

Codex CLI integration should wrap outbound context collection with `agent-privacy-guard inspect` and `sanitize --target codex`.

Posthook checks can be used before applying generated shell commands or patches.

# Codex CLI Adapter

Codex CLI integration では、outbound context collection を `agent-privacy-guard inspect` と `sanitize --target codex` で wrap します。

generated shell command や patch を適用する前に、posthook check を利用できます。

# Cursor Adapter

Cursor integration should call `agent-privacy-guard sanitize --target cursor` before external prompt submission.

This directory is intentionally small so Cursor-specific config does not leak into core policy logic.

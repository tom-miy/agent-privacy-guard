# Cursor Adapter

Cursor integration では、external prompt submission の前に `agent-privacy-guard sanitize --target cursor` を呼び出します。

この directory は意図的に小さく保ち、Cursor-specific config が core policy logic に漏れないようにしています。

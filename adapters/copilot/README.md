# GitHub Copilot Adapter

Copilot integration should use the shared sanitizer with `--target copilot`.

The minimal implementation demonstrates target-specific policy without pretending to be a complete Copilot proxy.
